version: "2"

linters:
  enable:
    - cyclop
    - unused     # unused unexported identifiers / dead code (staticcheck U1000)
  settings:
    cyclop:
      max-complexity: 15
      package-average: 8.0
